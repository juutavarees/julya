salario = float(input("Digite o salário inicial: "))
anos = int(input("Digite o número de anos: "))

aumento = 0.01  # 1% no primeiro ano

for _ in range(anos):
    salario += salario * aumento
    aumento *= 2  # Dobra a cada ano

print(f"Salário atual: R${salario:.2f}")
